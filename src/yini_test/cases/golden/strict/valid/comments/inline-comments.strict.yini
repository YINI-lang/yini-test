@yini strict

^ Application # Inline hash comment after section.
name = "strict-inline-comments" // Inline slash comment after string.
version = 1.2 # Inline hash comment after number.
enabled = true// Inline slash comment without spacing.

/END
